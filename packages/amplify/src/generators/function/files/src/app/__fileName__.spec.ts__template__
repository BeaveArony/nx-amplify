import { Context } from 'aws-lambda';
import { handler } from './<%= fileName %>';

describe('<%= propertyName %>', () => {
    it('handler should return result', async () => {
        expect(handler({}, {} as Context)).resolves.toEqual('<%= name %>');
    })
})
